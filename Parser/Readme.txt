Name : Pericherla Amshumaan Varma
Roll No : 17CS30025
Assignment No : 4
File : Readme File


make : to build the executable code to parse the sample test code.

make clean : to remove the files created while make

make test ; to test the parser with test file input.

In the test case, there is an error after for. We can see that the parser expects ( instead of . in the test file.

The parse prints the derivatio of each terminal on the terminal, till it encounters an error.
